version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ibovespa_db
    environment:
      POSTGRES_USER: ibovuser
      POSTGRES_PASSWORD: ibovpass
      POSTGRES_DB: ibovespa
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ibovuser"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: ibovespa_api
    environment:
      DATABASE_URL: postgresql://ibovuser:ibovpass@postgres:5432/ibovespa
      PLAYWRIGHT_TIMEOUT: 30000
      LOG_LEVEL: INFO
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
